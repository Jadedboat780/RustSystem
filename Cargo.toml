[package]
name = "oper_system"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.9.23"   #реализует базовый BIOS-загрузчик без каких-либо C-зависимостей


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
